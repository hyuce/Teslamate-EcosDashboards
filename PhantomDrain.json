{
  "uid": "ecos-pd-01",
  "title": "Phantom Drain",
  "tags": [
    "EcosDashboards"
  ],
  "timezone": "browser",
  "weekStart": "monday",
  "editable": true,
  "graphTooltip": 1,
  "schemaVersion": 40,
  "time": {
    "from": "now-6M",
    "to": "now"
  },
  "timepicker": {},
  "refresh": "",
  "preload": false,
  "fiscalYearStartMonth": 0,
  "links": [
    {
      "icon": "dashboard",
      "tags": [],
      "title": "TeslaMate",
      "tooltip": "",
      "type": "link",
      "url": "${base_url:raw}"
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "tags": [
        "tesla"
      ],
      "title": "Dashboards",
      "type": "dashboards"
    },
    {
      "asDropdown": true,
      "icon": "external link",
      "includeVars": false,
      "keepTime": false,
      "tags": [
        "EcosDashboards"
      ],
      "targetBlank": false,
      "title": "Ecos Dashboards",
      "tooltip": "",
      "type": "dashboards",
      "url": ""
    }
  ],
  "templating": {
    "list": [
      {
        "current": {},
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "TeslaMate"
        },
        "definition": "SELECT\n    id as __value,\n    CASE WHEN COUNT(id) OVER (PARTITION BY name) > 1 AND name IS NOT NULL THEN CONCAT(name, ' - ', RIGHT(vin, 6)) ELSE COALESCE(name, CONCAT('VIN ', vin)) end as __text\nFROM cars\nORDER BY display_priority ASC, name ASC, vin ASC",
        "includeAll": false,
        "label": "Car",
        "name": "car_id",
        "options": [],
        "query": "SELECT\n    id as __value,\n    CASE WHEN COUNT(id) OVER (PARTITION BY name) > 1 AND name IS NOT NULL THEN CONCAT(name, ' - ', RIGHT(vin, 6)) ELSE COALESCE(name, CONCAT('VIN ', vin)) end as __text\nFROM cars\nORDER BY display_priority ASC, name ASC, vin ASC",
        "refresh": 1,
        "regex": "",
        "type": "query"
      },
      {
        "current": {},
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "TeslaMate"
        },
        "definition": "SELECT unit_of_length FROM settings LIMIT 1",
        "hide": 2,
        "includeAll": false,
        "name": "length_unit",
        "options": [],
        "query": "SELECT unit_of_length FROM settings LIMIT 1",
        "refresh": 1,
        "regex": "",
        "type": "query"
      },
      {
        "current": {},
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "TeslaMate"
        },
        "definition": "SELECT unit_of_temperature FROM settings LIMIT 1",
        "hide": 2,
        "includeAll": false,
        "name": "temp_unit",
        "options": [],
        "query": "SELECT unit_of_temperature FROM settings LIMIT 1",
        "refresh": 1,
        "regex": "",
        "type": "query"
      },
      {
        "current": {},
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "TeslaMate"
        },
        "definition": "SELECT preferred_range FROM settings LIMIT 1",
        "hide": 2,
        "includeAll": false,
        "name": "preferred_range",
        "options": [],
        "query": "SELECT preferred_range FROM settings LIMIT 1",
        "refresh": 1,
        "regex": "",
        "type": "query"
      },
      {
        "current": {},
        "datasource": {
          "type": "grafana-postgresql-datasource",
          "uid": "TeslaMate"
        },
        "definition": "SELECT base_url FROM settings LIMIT 1",
        "hide": 2,
        "includeAll": false,
        "name": "base_url",
        "options": [],
        "query": "SELECT base_url FROM settings LIMIT 1",
        "refresh": 1,
        "regex": "",
        "type": "query"
      },
      {
        "current": {
          "selected": false,
          "text": "0",
          "value": "0"
        },
        "hide": 2,
        "includeAll": false,
        "label": "Efficiency (Wh/km)",
        "multi": false,
        "name": "efficiency",
        "options": [],
        "query": "SELECT efficiency FROM cars WHERE id = $car_id",
        "refresh": 2,
        "regex": "",
        "skipUrlSync": false,
        "sort": 0,
        "tagValuesQuery": "",
        "tags": [],
        "tagsQuery": "",
        "type": "query",
        "useTags": false
      }
    ]
  },
  "annotations": {
    "list": [
      {
        "builtIn": 1,
        "datasource": {
          "type": "grafana",
          "uid": "-- Grafana --"
        },
        "enable": true,
        "hide": true,
        "iconColor": "rgba(0, 211, 255, 1)",
        "name": "Annotations & Alerts",
        "type": "dashboard"
      }
    ]
  },
  "version": 1,
  "description": "Phantom drain analysis: how much range is lost between drives, why, and when. Part of Teslamate Ecos custom dashboards.",
  "panels": [
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 0,
        "w": 24,
        "h": 1
      },
      "id": 101,
      "panels": [],
      "title": "Summary",
      "type": "row"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "description": "Average range lost per day. Only gaps 30min–7 days. Zero-loss days included.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "yellow",
                "value": 3
              },
              {
                "color": "red",
                "value": 8
              }
            ]
          },
          "unit": "lengthkm",
          "custom": {}
        },
        "overrides": []
      },
      "gridPos": {
        "x": 0,
        "y": 1,
        "w": 6,
        "h": 3
      },
      "id": 200,
      "options": {
        "colorMode": "background",
        "graphMode": "area",
        "justifyMode": "auto",
        "textMode": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "11.5.2",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (SELECT d.start_date, d.end_date, convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range, LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range, EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours, d.end_${preferred_range}_range_km AS end_range_km, LEAD(d.start_${preferred_range}_range_km) OVER (ORDER BY d.start_date) AS next_start_range_km FROM drives d WHERE d.car_id = $car_id AND $__timeFilter(d.start_date) AND d.end_date IS NOT NULL AND d.start_${preferred_range}_range_km IS NOT NULL AND d.end_${preferred_range}_range_km IS NOT NULL) SELECT ROUND(AVG(loss)::numeric, 1) AS \"Avg Daily Loss ['$length_unit']\" FROM (SELECT end_date::date AS day, SUM(GREATEST(end_range - next_start_range, 0)) AS loss FROM drive_gaps WHERE next_start_range IS NOT NULL AND end_range > next_start_range AND gap_hours > 0.5 AND gap_hours < 168 GROUP BY 1) AS daily",
          "refId": "A"
        }
      ],
      "title": "Avg Daily Loss",
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "description": "Average range lost per hour while parked. Only gaps 30min–7 days.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "yellow",
                "value": 0.2
              },
              {
                "color": "red",
                "value": 0.5
              }
            ]
          },
          "unit": "velocitykmh",
          "custom": {}
        },
        "overrides": []
      },
      "gridPos": {
        "x": 6,
        "y": 1,
        "w": 6,
        "h": 3
      },
      "id": 201,
      "options": {
        "colorMode": "background",
        "graphMode": "area",
        "justifyMode": "auto",
        "textMode": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "11.5.2",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (SELECT d.start_date, d.end_date, convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range, LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range, EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours, d.end_${preferred_range}_range_km AS end_range_km, LEAD(d.start_${preferred_range}_range_km) OVER (ORDER BY d.start_date) AS next_start_range_km FROM drives d WHERE d.car_id = $car_id AND $__timeFilter(d.start_date) AND d.end_date IS NOT NULL AND d.start_${preferred_range}_range_km IS NOT NULL AND d.end_${preferred_range}_range_km IS NOT NULL) SELECT ROUND((SUM(GREATEST(end_range - next_start_range, 0)) / NULLIF(SUM(gap_hours), 0))::numeric, 2) AS \"Avg Hourly Loss ['$length_unit'/h]\" FROM drive_gaps WHERE next_start_range IS NOT NULL AND end_range > next_start_range AND gap_hours > 0.5 AND gap_hours < 168",
          "refId": "A"
        }
      ],
      "title": "Avg Hourly Loss",
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "description": "Total range lost to phantom drain. Only gaps 30min–7 days.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "yellow",
                "value": 100
              },
              {
                "color": "red",
                "value": 500
              }
            ]
          },
          "unit": "none",
          "custom": {}
        },
        "overrides": []
      },
      "gridPos": {
        "x": 12,
        "y": 1,
        "w": 6,
        "h": 3
      },
      "id": 202,
      "options": {
        "colorMode": "background",
        "graphMode": "area",
        "justifyMode": "auto",
        "textMode": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "11.5.2",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (SELECT d.start_date, d.end_date, convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range, LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range, EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours, d.end_${preferred_range}_range_km AS end_range_km, LEAD(d.start_${preferred_range}_range_km) OVER (ORDER BY d.start_date) AS next_start_range_km FROM drives d WHERE d.car_id = $car_id AND $__timeFilter(d.start_date) AND d.end_date IS NOT NULL AND d.start_${preferred_range}_range_km IS NOT NULL AND d.end_${preferred_range}_range_km IS NOT NULL) SELECT ROUND(SUM(GREATEST(end_range - next_start_range, 0))::numeric, 1) AS \"Total Lost ['$length_unit']\" FROM drive_gaps WHERE next_start_range IS NOT NULL AND end_range > next_start_range AND gap_hours > 0.5 AND gap_hours < 168",
          "refId": "A"
        }
      ],
      "title": "Total Range Lost",
      "type": "stat"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "description": "Estimated cumulative energy lost using $efficiency variable.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "thresholds"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "yellow",
                "value": 50
              },
              {
                "color": "red",
                "value": 200
              }
            ]
          },
          "unit": "kWh",
          "custom": {}
        },
        "overrides": []
      },
      "gridPos": {
        "x": 18,
        "y": 1,
        "w": 6,
        "h": 3
      },
      "id": 203,
      "options": {
        "colorMode": "background",
        "graphMode": "area",
        "justifyMode": "auto",
        "textMode": "auto",
        "reduceOptions": {
          "calcs": [
            "lastNotNull"
          ],
          "fields": "",
          "values": false
        }
      },
      "pluginVersion": "11.5.2",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (SELECT d.start_date, d.end_date, convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range, LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range, EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours, d.end_${preferred_range}_range_km AS end_range_km, LEAD(d.start_${preferred_range}_range_km) OVER (ORDER BY d.start_date) AS next_start_range_km FROM drives d WHERE d.car_id = $car_id AND $__timeFilter(d.start_date) AND d.end_date IS NOT NULL AND d.start_${preferred_range}_range_km IS NOT NULL AND d.end_${preferred_range}_range_km IS NOT NULL) SELECT ROUND((SUM(GREATEST(end_range_km - next_start_range_km, 0)) * $efficiency / 1000.0)::numeric, 1) AS \"Est. Energy Lost [kWh]\" FROM drive_gaps WHERE next_start_range_km IS NOT NULL AND end_range_km > next_start_range_km AND gap_hours > 0.5 AND gap_hours < 168",
          "refId": "A"
        }
      ],
      "title": "Est. Energy Lost",
      "type": "stat"
    },
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 4,
        "w": 24,
        "h": 1
      },
      "id": 102,
      "panels": [],
      "title": "Trends",
      "type": "row"
    },
    {
      "type": "timeseries",
      "title": "Daily Phantom Drain",
      "description": "Daily range lost while parked with 7-day rolling average. Only parking gaps 30min–7 days considered. Orange bars show daily loss, blue dashed line shows trend.",
      "gridPos": {
        "x": 0,
        "y": 4,
        "w": 24,
        "h": 8
      },
      "id": 204,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT\n    d.start_date,\n    d.end_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n),\ndaily_loss AS (\n  SELECT\n    end_date::date AS day,\n    SUM(GREATEST(end_range - next_start_range, 0)) AS loss\n  FROM drive_gaps\n  WHERE next_start_range IS NOT NULL\n    AND end_range > next_start_range\n    AND gap_hours > 0.5\n    AND gap_hours < 168\n  GROUP BY 1\n)\nSELECT\n  day AS time,\n  ROUND(loss::numeric, 1) AS \"Daily Loss [$length_unit]\",\n  ROUND(AVG(loss) OVER (ORDER BY day ROWS BETWEEN 6 PRECEDING AND CURRENT ROW)::numeric, 1) AS \"7-Day Avg\"\nFROM daily_loss\nORDER BY day",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "calcs": [
            "mean",
            "max",
            "lastNotNull"
          ]
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "lengthkm",
          "min": 0,
          "color": {
            "mode": "fixed",
            "fixedColor": "#E67E22"
          },
          "custom": {
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "bars",
            "fillOpacity": 70,
            "gradientMode": "opacity",
            "hideFrom": {
              "legend": false,
              "tooltip": false,
              "viz": false
            },
            "lineInterpolation": "smooth",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": true,
            "stacking": {
              "group": "A",
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "#E67E22",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "7-Day Avg"
            },
            "properties": [
              {
                "id": "color",
                "value": {
                  "fixedColor": "#1F78B4",
                  "mode": "fixed"
                }
              },
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.lineWidth",
                "value": 3
              },
              {
                "id": "custom.pointSize",
                "value": 0
              },
              {
                "id": "custom.showPoints",
                "value": "never"
              },
              {
                "id": "custom.lineStyle",
                "value": {
                  "dash": [
                    10,
                    10
                  ],
                  "fill": "dash"
                }
              },
              {
                "id": "custom.fillOpacity",
                "value": 0
              },
              {
                "id": "custom.spanNulls",
                "value": true
              }
            ]
          }
        ]
      }
    },
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 11,
        "w": 24,
        "h": 1
      },
      "id": 103,
      "panels": [],
      "title": "Patterns",
      "type": "row"
    },
    {
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "description": "Average drain by day of week. Higher values may indicate Sentry Mode or scheduled events.",
      "fieldConfig": {
        "defaults": {
          "color": {
            "mode": "fixed",
            "fixedColor": "orange"
          },
          "custom": {
            "fillOpacity": 80,
            "gradientMode": "scheme",
            "hideFrom": {
              "legend": false,
              "tooltip": false,
              "viz": false
            },
            "lineWidth": 0,
            "scaleDistribution": {
              "type": "linear"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          },
          "unit": "lengthkm"
        },
        "overrides": []
      },
      "gridPos": {
        "x": 0,
        "y": 11,
        "w": 12,
        "h": 7
      },
      "id": 206,
      "options": {
        "barRadius": 0,
        "barWidth": 0.55,
        "fullHighlight": true,
        "groupWidth": 0.75,
        "legend": {
          "calcs": [
            "mean"
          ],
          "displayMode": "list",
          "placement": "bottom",
          "showLegend": true
        },
        "orientation": "vertical",
        "showValue": "always",
        "stacking": "none",
        "tooltip": {
          "mode": "single",
          "sort": "none"
        },
        "xTickLabelRotation": 0,
        "xTickLabelSpacing": 0,
        "xField": "Day"
      },
      "pluginVersion": "11.5.2",
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (SELECT d.start_date, d.end_date, convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range, LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range, EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours, d.end_${preferred_range}_range_km AS end_range_km, LEAD(d.start_${preferred_range}_range_km) OVER (ORDER BY d.start_date) AS next_start_range_km FROM drives d WHERE d.car_id = $car_id AND $__timeFilter(d.start_date) AND d.end_date IS NOT NULL AND d.start_${preferred_range}_range_km IS NOT NULL AND d.end_${preferred_range}_range_km IS NOT NULL) SELECT CASE EXTRACT(DOW FROM end_date) WHEN 0 THEN 'Sun' WHEN 1 THEN 'Mon' WHEN 2 THEN 'Tue' WHEN 3 THEN 'Wed' WHEN 4 THEN 'Thu' WHEN 5 THEN 'Fri' WHEN 6 THEN 'Sat' END AS \"Day\", ROUND(AVG(GREATEST(end_range - next_start_range, 0))::numeric, 1) AS \"Avg Loss ['$length_unit']\" FROM drive_gaps WHERE next_start_range IS NOT NULL AND end_range > next_start_range AND gap_hours > 0.5 AND gap_hours < 168 GROUP BY EXTRACT(DOW FROM end_date) ORDER BY EXTRACT(DOW FROM end_date)",
          "refId": "A"
        }
      ],
      "title": "By Day of Week",
      "type": "barchart"
    },
    {
      "type": "barchart",
      "title": "Drain Rate by Temperature",
      "description": "Average hourly range loss by outside temperature. Cold (<5°C) and hot (>25°C) extremes increase phantom drain due to battery thermal management. Events = sample count per bucket.",
      "gridPos": {
        "x": 12,
        "y": 11,
        "w": 12,
        "h": 7
      },
      "id": 207,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT\n    d.start_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours,\n    LEAD(d.outside_temp_avg) OVER (ORDER BY d.start_date) AS next_outside_temp\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n)\nSELECT\n  CASE\n    WHEN next_outside_temp < 0 THEN '<0°C'\n    WHEN next_outside_temp < 5 THEN '0-5°C'\n    WHEN next_outside_temp < 10 THEN '5-10°C'\n    WHEN next_outside_temp < 15 THEN '10-15°C'\n    WHEN next_outside_temp < 20 THEN '15-20°C'\n    WHEN next_outside_temp < 25 THEN '20-25°C'\n    ELSE '25°C+'\n  END AS \"Temperature\",\n  ROUND(AVG(GREATEST(end_range - next_start_range, 0) / NULLIF(gap_hours, 0))::numeric, 3) AS \"Avg Hourly Drain\",\n  COUNT(*)::int AS \"Events\"\nFROM drive_gaps\nWHERE next_start_range IS NOT NULL\n  AND end_range > next_start_range\n  AND gap_hours > 0.5\n  AND gap_hours < 168\n  AND next_outside_temp IS NOT NULL\nGROUP BY 1\nORDER BY MIN(next_outside_temp)",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "vertical",
        "xField": "Temperature",
        "showValue": "always",
        "stacking": "none",
        "groupWidth": 0.7,
        "barWidth": 0.85,
        "barRadius": 0,
        "fullHighlight": false,
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "show": false
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "velocitykmh",
          "min": 0,
          "decimals": 3,
          "color": {
            "mode": "fixed",
            "fixedColor": "#E67E22"
          },
          "custom": {
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "bars",
            "fillOpacity": 90,
            "gradientMode": "none",
            "hideFrom": {
              "tooltip": false,
              "viz": false,
              "legend": false
            },
            "lineInterpolation": "linear",
            "lineWidth": 0,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": {
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "transparent"
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Events"
            },
            "properties": [
              {
                "id": "unit",
                "value": "short"
              },
              {
                "id": "decimals",
                "value": 0
              },
              {
                "id": "color",
                "value": {
                  "fixedColor": "#8F8F8F",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      }
    },
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 19,
        "w": 24,
        "h": 1
      },
      "id": 104,
      "panels": [],
      "title": "Details",
      "type": "row"
    },
    {
      "type": "table",
      "title": "Worst Drain Days",
      "description": "Top 20 days with highest phantom drain. Shows date, range lost, park duration, hourly rate, and temperature.",
      "gridPos": {
        "x": 0,
        "y": 19,
        "w": 12,
        "h": 10
      },
      "id": 211,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT \n    d.start_date,\n    d.end_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours,\n    d.outside_temp_avg\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n)\nSELECT \n  DATE(end_date) AS \"Date\",\n  ROUND(SUM(GREATEST(end_range - next_start_range, 0))::numeric, 2) AS \"Range Lost [$length_unit]\",\n  ROUND(AVG(gap_hours)::numeric, 1) AS \"Park Hours [h]\",\n  ROUND((SUM(GREATEST(end_range - next_start_range, 0)) / NULLIF(AVG(gap_hours), 0))::numeric, 2) AS \"Hourly Rate [$length_unit/h]\",\n  ROUND(AVG(outside_temp_avg)::numeric, 1) AS \"Temp [°C]\"\nFROM drive_gaps\nWHERE next_start_range IS NOT NULL\n  AND end_range > next_start_range\n  AND gap_hours > 0.5\n  AND gap_hours < 168\nGROUP BY 1\nORDER BY \"Range Lost [$length_unit]\" DESC\nLIMIT 20",
          "refId": "A"
        }
      ],
      "options": {
        "cellHeight": "sm",
        "footer": {
          "show": false
        },
        "showHeader": true,
        "sortBy": [
          {
            "displayName": "Range Lost",
            "desc": true
          }
        ]
      },
      "fieldConfig": {
        "defaults": {
          "custom": {
            "align": "center",
            "cellOptions": {
              "type": "auto"
            },
            "displayMode": "auto"
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Hourly Rate"
            },
            "properties": [
              {
                "id": "custom.cellOptions",
                "value": {
                  "type": "color-background"
                }
              },
              {
                "id": "custom.displayMode",
                "value": "gradient"
              },
              {
                "id": "color",
                "value": {
                  "mode": "continuous-RdYlGr"
                }
              },
              {
                "id": "min",
                "value": 0
              },
              {
                "id": "max",
                "value": 5
              }
            ]
          }
        ]
      }
    },
    {
      "type": "barchart",
      "title": "Drain Rate by Vehicle State",
      "description": "Average hourly range loss per parked state. Asleep = deep sleep (lowest), Online = Sentry/climate active (highest), Offline = unreachable. Events = sample count.",
      "gridPos": {
        "x": 12,
        "y": 19,
        "w": 12,
        "h": 10
      },
      "id": 212,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT\n    d.start_date,\n    d.end_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n),\ngap_with_state AS (\n  SELECT\n    dg.gap_hours,\n    GREATEST(dg.end_range - dg.next_start_range, 0) AS range_loss,\n    (SELECT s.state::text FROM states s\n     WHERE s.car_id = $car_id\n       AND s.start_date <= dg.end_date\n       AND (s.end_date IS NULL OR s.end_date >= dg.end_date)\n     ORDER BY s.start_date DESC\n     LIMIT 1) AS state\n  FROM drive_gaps dg\n  WHERE dg.next_start_range IS NOT NULL\n    AND dg.end_range > dg.next_start_range\n    AND dg.gap_hours > 0.5\n    AND dg.gap_hours < 168\n)\nSELECT\n  COALESCE(state, 'unknown') AS \"State\",\n  ROUND(AVG(range_loss / NULLIF(gap_hours, 0))::numeric, 3) AS \"Avg Hourly Drain\",\n  COUNT(*)::int AS \"Events\"\nFROM gap_with_state\nGROUP BY 1\nORDER BY \"Avg Hourly Drain\" DESC",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "horizontal",
        "xField": "State",
        "showValue": "always",
        "stacking": "none",
        "groupWidth": 0.7,
        "barWidth": 0.85,
        "barRadius": 0,
        "fullHighlight": false,
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "show": false
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "velocitykmh",
          "min": 0,
          "decimals": 3,
          "color": {
            "mode": "fixed",
            "fixedColor": "#E67E22"
          },
          "custom": {
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "bars",
            "fillOpacity": 90,
            "gradientMode": "none",
            "hideFrom": {
              "tooltip": false,
              "viz": false,
              "legend": false
            },
            "lineInterpolation": "linear",
            "lineWidth": 0,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": {
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "transparent"
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Events"
            },
            "properties": [
              {
                "id": "unit",
                "value": "short"
              },
              {
                "id": "decimals",
                "value": 0
              },
              {
                "id": "color",
                "value": {
                  "fixedColor": "#8F8F8F",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      }
    },
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 29,
        "w": 24,
        "h": 1
      },
      "id": 105,
      "panels": [],
      "title": "Park Duration Analysis",
      "type": "row"
    },
    {
      "type": "barchart",
      "title": "Drain by Park Duration",
      "description": "Average total range lost per parking duration. Event count in tooltip shows sample size per bucket.",
      "gridPos": {
        "x": 0,
        "y": 29,
        "w": 12,
        "h": 7
      },
      "id": 214,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT\n    d.start_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n)\nSELECT\n  CASE\n    WHEN gap_hours < 1 THEN '<1h'\n    WHEN gap_hours < 2 THEN '1-2h'\n    WHEN gap_hours < 4 THEN '2-4h'\n    WHEN gap_hours < 8 THEN '4-8h'\n    WHEN gap_hours < 12 THEN '8-12h'\n    WHEN gap_hours < 24 THEN '12-24h'\n    WHEN gap_hours < 48 THEN '1-2d'\n    ELSE '>2d'\n  END AS \"Duration\",\n  ROUND(AVG(GREATEST(end_range - next_start_range, 0))::numeric, 2) AS \"Avg Drain\",\n  ROUND((SUM(GREATEST(end_range - next_start_range, 0)) / NULLIF(SUM(gap_hours), 0))::numeric, 3) AS \"Hourly Rate\",\n  COUNT(*) AS \"Events\"\nFROM drive_gaps\nWHERE next_start_range IS NOT NULL\n  AND end_range > next_start_range\n  AND gap_hours > 0.5\n  AND gap_hours < 168\nGROUP BY 1\nORDER BY MIN(gap_hours)",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "vertical",
        "xField": "Duration",
        "xTickLabelRotation": 0,
        "showValue": "auto",
        "stacking": "none",
        "groupWidth": 0.7,
        "barWidth": 0.8,
        "barRadius": 0,
        "fullHighlight": false,
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "show": false
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "lengthkm",
          "min": 0,
          "color": {
            "mode": "fixed",
            "fixedColor": "orange"
          },
          "custom": {
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "bars",
            "fillOpacity": 80,
            "gradientMode": "none",
            "hideFrom": {
              "tooltip": false,
              "viz": false,
              "legend": false
            },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": {
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "transparent"
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Events"
            },
            "properties": [
              {
                "id": "custom.hideFrom",
                "value": {
                  "viz": true,
                  "legend": false,
                  "tooltip": false
                }
              },
              {
                "id": "unit",
                "value": "short"
              },
              {
                "id": "decimals",
                "value": 0
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Hourly Rate"
            },
            "properties": [
              {
                "id": "custom.hideFrom",
                "value": {
                  "viz": true,
                  "legend": false,
                  "tooltip": false
                }
              }
            ]
          }
        ]
      }
    },
    {
      "type": "barchart",
      "title": "Drain Rate by Park Duration",
      "description": "Average hourly drain rate per parking duration. Higher rates suggest Sentry Mode, climate, or extreme weather.",
      "gridPos": {
        "x": 12,
        "y": 29,
        "w": 12,
        "h": 7
      },
      "id": 219,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "table",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT\n    d.start_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n)\nSELECT\n  CASE\n    WHEN gap_hours < 1 THEN '<1h'\n    WHEN gap_hours < 2 THEN '1-2h'\n    WHEN gap_hours < 4 THEN '2-4h'\n    WHEN gap_hours < 8 THEN '4-8h'\n    WHEN gap_hours < 12 THEN '8-12h'\n    WHEN gap_hours < 24 THEN '12-24h'\n    WHEN gap_hours < 48 THEN '1-2d'\n    ELSE '>2d'\n  END AS \"Duration\",\n  ROUND(AVG(GREATEST(end_range - next_start_range, 0))::numeric, 2) AS \"Avg Drain\",\n  ROUND((SUM(GREATEST(end_range - next_start_range, 0)) / NULLIF(SUM(gap_hours), 0))::numeric, 3) AS \"Hourly Rate\",\n  COUNT(*) AS \"Events\"\nFROM drive_gaps\nWHERE next_start_range IS NOT NULL\n  AND end_range > next_start_range\n  AND gap_hours > 0.5\n  AND gap_hours < 168\nGROUP BY 1\nORDER BY MIN(gap_hours)",
          "refId": "A"
        }
      ],
      "options": {
        "orientation": "vertical",
        "xField": "Duration",
        "xTickLabelRotation": 0,
        "showValue": "auto",
        "stacking": "none",
        "groupWidth": 0.7,
        "barWidth": 0.8,
        "barRadius": 0,
        "fullHighlight": false,
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "show": false
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "velocitykmh",
          "min": 0,
          "decimals": 3,
          "color": {
            "mode": "palette-classic"
          },
          "custom": {
            "axisPlacement": "auto",
            "barAlignment": 0,
            "drawStyle": "bars",
            "fillOpacity": 80,
            "gradientMode": "none",
            "hideFrom": {
              "tooltip": false,
              "viz": false,
              "legend": false
            },
            "lineInterpolation": "linear",
            "lineWidth": 1,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": {
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "transparent"
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "Events"
            },
            "properties": [
              {
                "id": "custom.hideFrom",
                "value": {
                  "viz": true,
                  "legend": false,
                  "tooltip": false
                }
              },
              {
                "id": "unit",
                "value": "short"
              },
              {
                "id": "decimals",
                "value": 0
              }
            ]
          },
          {
            "matcher": {
              "id": "byName",
              "options": "Avg Drain"
            },
            "properties": [
              {
                "id": "custom.hideFrom",
                "value": {
                  "viz": true,
                  "legend": false,
                  "tooltip": false
                }
              }
            ]
          }
        ]
      }
    },
    {
      "collapsed": false,
      "gridPos": {
        "x": 0,
        "y": 36,
        "w": 24,
        "h": 1
      },
      "id": 106,
      "panels": [],
      "title": "Long Term",
      "type": "row"
    },
    {
      "type": "timeseries",
      "title": "Monthly Drain Trend",
      "description": "Monthly total range lost with 3-month moving average. Shows seasonal patterns.",
      "gridPos": {
        "x": 0,
        "y": 36,
        "w": 24,
        "h": 8
      },
      "id": 215,
      "datasource": {
        "type": "grafana-postgresql-datasource",
        "uid": "TeslaMate"
      },
      "targets": [
        {
          "datasource": {
            "type": "grafana-postgresql-datasource",
            "uid": "TeslaMate"
          },
          "editorMode": "code",
          "format": "time_series",
          "rawQuery": true,
          "rawSql": "WITH drive_gaps AS (\n  SELECT \n    d.start_date,\n    d.end_date,\n    convert_km(d.end_${preferred_range}_range_km, '$length_unit') AS end_range,\n    LEAD(convert_km(d.start_${preferred_range}_range_km, '$length_unit')) OVER (ORDER BY d.start_date) AS next_start_range,\n    EXTRACT(EPOCH FROM (LEAD(d.start_date) OVER (ORDER BY d.start_date) - d.end_date)) / 3600 AS gap_hours\n  FROM drives d\n  WHERE d.car_id = $car_id\n    AND $__timeFilter(d.start_date)\n    AND d.end_date IS NOT NULL\n    AND d.start_${preferred_range}_range_km IS NOT NULL\n    AND d.end_${preferred_range}_range_km IS NOT NULL\n),\nmonthly AS (\n  SELECT \n    DATE_TRUNC('month', end_date) AS month,\n    SUM(GREATEST(end_range - next_start_range, 0)) AS total_loss\n  FROM drive_gaps\n  WHERE next_start_range IS NOT NULL\n    AND end_range > next_start_range\n    AND gap_hours > 0.5\n    AND gap_hours < 168\n  GROUP BY 1\n)\nSELECT \n  month AS time,\n  total_loss AS \"Monthly Loss\",\n  AVG(total_loss) OVER (ORDER BY month ROWS BETWEEN 2 PRECEDING AND CURRENT ROW) AS \"3-Month Avg\"\nFROM monthly\nORDER BY month",
          "refId": "A"
        }
      ],
      "options": {
        "tooltip": {
          "mode": "multi",
          "sort": "desc"
        },
        "legend": {
          "displayMode": "list",
          "placement": "bottom",
          "calcs": [
            "mean",
            "sum"
          ]
        }
      },
      "fieldConfig": {
        "defaults": {
          "unit": "lengthkm",
          "min": 0,
          "color": {
            "mode": "fixed",
            "fixedColor": "orange"
          },
          "custom": {
            "axisPlacement": "auto",
            "drawStyle": "bars",
            "fillOpacity": 70,
            "gradientMode": "none",
            "hideFrom": {
              "tooltip": false,
              "viz": false,
              "legend": false
            },
            "lineInterpolation": "smooth",
            "lineWidth": 2,
            "pointSize": 5,
            "scaleDistribution": {
              "type": "linear"
            },
            "showPoints": "never",
            "spanNulls": false,
            "stacking": {
              "mode": "none"
            },
            "thresholdsStyle": {
              "mode": "off"
            }
          },
          "mappings": [],
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              }
            ]
          }
        },
        "overrides": [
          {
            "matcher": {
              "id": "byName",
              "options": "3-Month Avg"
            },
            "properties": [
              {
                "id": "custom.drawStyle",
                "value": "line"
              },
              {
                "id": "custom.fillOpacity",
                "value": 0
              },
              {
                "id": "custom.lineWidth",
                "value": 2
              },
              {
                "id": "color",
                "value": {
                  "fixedColor": "orange",
                  "mode": "fixed"
                }
              }
            ]
          }
        ]
      }
    }
  ]
}